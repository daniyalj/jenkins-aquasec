apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: aqua-pipeline
spec:
  failedBuildsHistoryLimit: 5
  nodeSelector: {}
  output: {}
  postCommit: {}
  resources: {}
  runPolicy: Serial
  source:
    type: None
  strategy:
    jenkinsPipelineStrategy:
      jenkinsfile: |-
        pipeline {
            agent any

            stages {
                stage('Scan') {
                    steps {
                        script {
                            aqua customFlags: '', hideBase: false, hostedImage: 'test-project/random-beer-aqua', localImage: '', locationType: 'hosted', notCompliesCmd: '', onDisallowed: 'fail', policies: '', register: false, registry: 'OpenShift Internal Registry', showNegligible: false                }
                    }
                }
             }
           }
    type: JenkinsPipeline
  triggers: []
